# Scratch #

This is a project used to test the locally built source generator (debug).  

After building the package (debug), use the ResetNuget.bat file to reset the nuget cache to have the latest debug build,
and then load/open the scratch project in a separate VS Code window.  



